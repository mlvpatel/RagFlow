# rag-naive-2022, naive RAG baseline (2022 generation).
# The architecture is the 2022 naive pattern; the library versions are current
# and security patched, verified with pip-audit.

# Web service and UI
fastapi==0.139.0
uvicorn[standard]==0.50.2
streamlit==1.58.0
python-multipart==0.0.32

# Config, security, utilities
pydantic-settings==2.14.2
slowapi==0.1.10
bleach==6.4.0
requests==2.34.2
python-dotenv==1.2.2

# RAG core: LangChain with Chroma. No reranker, no hybrid search.
langchain-core==1.4.8
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-openai==1.3.3
langchain-ollama==1.1.0
chromadb==1.5.9
pypdf==6.14.2

# Transitive dependencies pinned to patched versions for security.
pillow>=12.2.0
# 83.0.0 patches PYSEC-2026-3447. A lower floor is satisfied by the runner's
# preinstalled setuptools, so pip never upgrades and the vulnerable one stays.
setuptools>=83.0.0
